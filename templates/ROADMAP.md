# Roadmap

> Short-term goals. For long-term project goals see PROJECT.md.
>
> This file holds only the current milestone. Open questions live in
> `questions/` (Q-NNN); committed designs in `decisions/` (D-NNN);
> finished work in closed journal entries + `git log`. Resist adding
> "Up next" / "Backlog" / "Recently done" sections — they duplicate
> state that already has a single source of truth elsewhere and rot.

## Current milestone

(none yet — describe the goal narrative and exit criteria here)
