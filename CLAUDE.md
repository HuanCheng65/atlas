# Agent Rules

## Project context

See PROJECT.md for background and long-term goals.

<!-- atlas-init: BEGIN (managed block; delete entire block + re-run atlas-init to refresh) -->
## Operational memory (atlas)

This project uses atlas at `docs/atlas/`. **At session start, invoke the `using-atlas` skill** — it orchestrates state loading, intent detection, and journal management.

See `docs/atlas/README.md` for the data model.
<!-- atlas-init: END -->

## Coding conventions

(your conventions here)
